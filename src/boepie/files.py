from collections.abc import Sequence
from pathlib import Path

# def parse_ms_path(path: str | Path) -> tuple[Path, str]


# def fetch_columns_from_measurement_set(ms_path: str | Path, columns: Sequence[str], subtable: str | None = None) -> dict[str, Any]:
